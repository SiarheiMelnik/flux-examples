'use strict';

import React from 'react';
import WeatherCard from './WeatherCard';

export default React.createClass({

  render() {
    return (
      <div>
        <WeatherCard />
      </div>
    );
  }
});
